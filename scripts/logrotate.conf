# BioPulse 应用日志轮转配置
# 安装: sudo ln -s /path/to/scripts/logrotate.conf /etc/logrotate.d/biopulse

/var/log/biopulse/*.log {
    daily
    rotate 30
    compress
    delaycompress
    copytruncate
    missingok
    notifempty
    dateext
    dateformat -%Y%m%d
}