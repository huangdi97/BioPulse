name: CI
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Python deps
        run: |
          pip install -r requirements.txt
          pip install ruff
      - name: Ruff check
        run: ruff check
      - name: Pytest
        run: python -m pytest cloud/app/tests/test_auth.py cloud/app/tests/test_compliance.py cloud/app/tests/test_mdt.py cloud/app/tests/test_memory.py cloud/app/tests/test_repositories.py cloud/app/tests/test_research_trajectory.py cloud/app/tests/test_sage_engine.py cloud/app/tests/test_decision.py cloud/app/tests/test_market.py cloud/app/tests/test_e2e_scope_isolation.py -v --tb=short --no-cov

  web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install
        run: npm ci
      - name: Build
        run: npm run build

  mobile:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile_app
    steps:
      - uses: actions/checkout@v4
      - name: dart analyze
        run: |
          if command -v dart; then
            dart analyze
          else
            echo "dart not available, skipping"
          fi

  docker:
    runs-on: ubuntu-latest
    needs: [test, web]
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker images
        run: docker compose build cloud sales-coach opportunity assistant sales-assistant pharma-intel management

  deploy:
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    needs: [docker]
    steps:
      - uses: actions/checkout@v4
      - name: Deploy via SSH
        env:
          SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
          SSH_HOST: ${{ secrets.DEPLOY_HOST }}
          SSH_USER: ${{ secrets.DEPLOY_USER }}
          SSH_PORT: ${{ secrets.DEPLOY_PORT }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh -i ~/.ssh/deploy_key -p $SSH_PORT -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST 'cd /home/hermes/one-cloud-four-ends && git pull && ./scripts/deploy.sh'
