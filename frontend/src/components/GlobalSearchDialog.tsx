import { SearchBar } from "@/components/SearchBar";
import { useEffect, useState } from "react";
export function GlobalSearchDialog({ open, onClose }: { open: boolean; onClose: () => void }) { const [query, setQuery] = useState(""); useEffect(() => { const handler = (e: KeyboardEvent) => { if (e.key === "Escape") onClose() }; if (open) { document.addEventListener("keydown", handler); return () => document.removeEventListener("keydown", handler) } }, [open, onClose]); if (!open) return null; return (<div className="fixed inset-0 z-50 flex items-start justify-center pt-[15vh] bg-black/40" onClick={onClose}><div className="w-full max-w-lg bg-white rounded-lg shadow-2xl p-4" onClick={(e) => e.stopPropagation()}><SearchBar value={query} onChange={setQuery} placeholder="搜索客户、拜访记录..." className="w-full" /><div className="mt-4 text-sm text-gray-400 text-center py-8">输入关键词开始搜索</div></div></div>) } export default GlobalSearchDialog;